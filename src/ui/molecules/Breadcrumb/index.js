import Breadcrumb from './Breadcrumb';
import { 
  BREADCRUMB_VARIANTS, 
  BREADCRUMB_SIZES, 
  BREADCRUMB_SEPARATOR_TYPES,
  DEFAULT_PROPS
} from './constants';

export default Breadcrumb;
export {
  BREADCRUMB_VARIANTS,
  BREADCRUMB_SIZES,
  BREADCRUMB_SEPARATOR_TYPES
};
