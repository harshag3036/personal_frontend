/**
 * StatusBadge Component Exports
 * 
 * This file exports the StatusBadge component and its related constants.
 */

import StatusBadge from './StatusBadge';
import { 
  STATUS_BADGE_CLASS, 
  STATUS_TYPES, 
  STATUS_DEFINITIONS,
  STATUS_BADGE_SIZES,
  STATUS_BADGE_MODIFIERS
} from './constants';

// Export the component as default
export default StatusBadge;

// Export constants for external use
export {
  STATUS_BADGE_CLASS,
  STATUS_TYPES,
  STATUS_DEFINITIONS,
  STATUS_BADGE_SIZES,
  STATUS_BADGE_MODIFIERS
};
